Hello BC
<%flash.each do |key, value| %>
	<div class="alert alert-<%= %>">
		<%= value %>
	</div>
<% end %>

<%= simple_form_for @tweet do |f| %>
  <%= f.input :content %>
  <%= f.button :submit %>
<% end %>